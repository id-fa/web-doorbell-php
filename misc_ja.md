# 簡易ドアベルシステム

## 拡張用ドキュメント

### 外部連携API（Slack 等から通知・応答する）

`integration_api` を `true` にすると、呼び出しを外部サービスへ通知し、外部から応答を返せる
**連携トークン**をID発行画面から発行できます。Slack への通知と Slack からの応対は、この上に
薄い中継を書いて実現します（Slack 固有の処理はこのリポジトリには含みません）。

1. ID発行画面の一覧から対象のIDを選び、詳細画面を開く
2. 「外部連携」の欄で連携の名前（「Slack #受付」など）と、必要なら**通知先URL**を入力して発行する
3. 表示された**連携トークン**と**署名シークレット**を控える（再表示できません）

通知先URLを空にすると「受信専用」の連携になり、通知は送らず API からの応答だけを受け付けます。

#### 送信（呼び出し → 通知先URL）

呼び出し・応答・不在確定のたびに、通知先URLへ次の JSON を POST します。

```json
{
  "text": "🔔 正面玄関 から呼び出しがあります [1234-5678]",
  "event": "call",
  "doorbellId": "1234-5678",
  "occurredAt": 1786198802,
  "call": {
    "id": 42, "childName": "正面玄関", "status": "waiting", "callCount": 1,
    "createdAt": 1786198802, "lastCalledAt": 1786198802, "expiresAt": 1786198862,
    "respondedAt": null, "responder": null, "message": null
  },
  "responses": { "in1": "1分以内", "in5": "5分以内", "away": "離席中" }
}
```

先頭の `text` は人が読むための1行です。**Slack の Incoming Webhook はこれを本文として表示するため、
通知先URLに `https://hooks.slack.com/services/…` をそのまま指定すれば中継なしで通知が届きます。**
自前の受信側を書く場合は `text` を無視して、`event` と `call` の構造化された値を使ってください。
子機名・応答者名は Slack の記法として解釈されないようエスケープしています。

| `event` | 発生タイミング |
| --- | --- |
| `call` | 子機がコールした（応答待ち中の連打も含む。まとめられた回数は `callCount`） |
| `answered` | 親機または連携が応答した |
| `no_answer` | 不在判定秒数を過ぎて応答なしで確定した |

リクエストヘッダで送信元を検証できます。

| ヘッダ | 内容 |
| --- | --- |
| `X-Doorbell-Event` | イベント種別 |
| `X-Doorbell-Delivery` | 配送ID（再送時も同じ値。重複処理の判定に使えます） |
| `X-Doorbell-Signature` | `sha256=` + 署名シークレットでボディを HMAC-SHA256 した16進文字列 |

```php
// 受信側での検証例
$expected = 'sha256=' . hash_hmac('sha256', $rawBody, $secret);
if (!hash_equals($expected, $_SERVER['HTTP_X_DOORBELL_SIGNATURE'] ?? '')) { /* 破棄 */ }
```

**cron は不要です。** 通知は一度データベースに積まれ、次に誰かがアクセスしたリクエストの
**レスポンスを返し切ったあと**に送信されます。親機・子機が `polling_interval` ごとに
アクセスしているため、常駐プロセスがなくても数秒以内に配送されます。
失敗した場合は間隔を空けて再送し、`webhook_max_attempts` 回で諦めます（ログに残します）。

#### 受信（外部 → 応答）

`integration.php` に POST します。Cookie もセッションも使わないため、CSRF トークンは不要です。

```bash
curl -X POST https://example.com/integration.php \
     -H 'Authorization: Bearer dbi_...' \
     -H 'Content-Type: application/json' \
     -d '{"action":"state"}'
```

| `action` | 内容 |
| --- | --- |
| `state` | 応答待ちの呼び出し・子機の稼働状態・履歴を返す（親機の画面状態と同じ内容） |
| `respond` | `call_id` と `response`（応答のキー。既定は `in1` / `in5` / `away`）で応答する。`responder` に応答者名を渡せる |

**応答のキーは設定 `responses` で変えられます。** `state` の戻り値に含まれる `responses`
（キー => 短縮ラベル）を見て組み立ててください。文言はサーバー側の定義のみを使います
（任意の文字列は送れません）。
`Authorization` ヘッダが PHP まで届かない環境では、リクエストボディの `token` でも渡せます
（URLのクエリでは受け付けません。アクセスログにトークンが残るためです）。

**このIDに連携があると、子機からは常に「親機オンライン」として扱われます。**
親機がポーリングしていなくても、外部から応答が返る可能性があるためです。

**セキュリティ上の注意**

- **連携トークンはそれ自体がログイン情報**です。トークンを持つ相手はそのIDの呼び出し状況を
  取得でき、応答も返せます（ID・パスワードは含まれないため、他のIDには影響しません）
- 既定では無効です。必要な場合だけ `config.php` で有効にしてください
- トークンはハッシュ化して保存されるため、発行時以外は確認できません。
  紛失したら失効させて発行し直してください
- 通知先URLは **https 限定**で、`webhook_allowed_hosts`（既定 `hooks.slack.com`）に
  載っているホストしか登録できません。サーバーを踏み台にして内部ネットワークへ
  リクエストを送られるのを防ぐためです。リダイレクトも追いません
- 認証の失敗はログインとは別枠でレート制限します（`rate_limit.max_logins` を流用）

#### Slack と繋ぐ

**通知を Slack に流すだけなら、追加のプログラムは要りません。**
Slack で Incoming Webhook を作り、そのURLを連携の通知先URLに指定してください。

```
🔔 正面玄関 から呼び出しがあります [1234-5678]
✅ 正面玄関 の呼び出しに 受付デスク が応答しました：1分以内に応対します [1234-5678]
⚠️ 正面玄関 の呼び出しに応答がないまま終了しました（2回） [1234-5678]
```

**Slack のボタンで応答まで行う場合**は、自前の中継を1つ用意します。
Slack の署名検証・Block Kit・「3秒以内に応答を返す」制約は Slack 側の都合が強く、
ワークスペースごとの設定にも依存するため、本体には含めていません。

**サンプルを `examples/slack_integration_sample.php` に置いてあります。**
設定を書き換えて公開ディレクトリに置けば動きます（設置手順はファイル冒頭のコメントを参照）。

```
子機がコール
  → [ドアベル] --通知 POST--> [中継] --> Slack にボタン付きメッセージ
  ← [ドアベル] <--respond---- [中継] <-- Slack でボタンが押される
```

中継がやることは 3 つだけです。

1. 通知の `X-Doorbell-Signature` を検証する
2. `call.id` と `responses` から Block Kit のボタン付きメッセージを組み立てて Slack へ送る
3. ボタンが押されたら `integration.php` の `respond` を
   `call_id` ＋ 押されたボタンのキー ＋ 押した人の名前で呼ぶ

### デモとして公開する（管理パスワードを共有する場合）

不特定多数に管理パスワードを教えて試してもらう設置では、**他人の設定を覗く・消す**ことが
できてしまいます。それを抑えるための設定が 4 つあります。いずれも既定では無効です。

```php
'mask_secrets'           => true,   // 子機URL・通知先URLを伏せ字にする
'mask_client_ip'         => true,   // IDの発行元IPを伏せ字にする
'deletion_grace_seconds' => 600,    // 発行から10分は削除・失効できない
'id_lifetime'            => 28800,  // 発行から8時間でIDを自動削除
```

| 設定 | 効果 |
| --- | --- |
| `mask_secrets` | 詳細画面の子機URLのトークンと外部連携の通知先URLを `1d928b…（以下伏せ字）` のように表示します。**発行した直後だけは全体を表示**するので、発行した本人はその場で控えられます。伏せ字のあいだはコピーボタンも出しません |
| `mask_client_ip` | 詳細画面の発行元IPを `192.0.x.x`（IPv6 は `2001:db8:…`）のように表示します。誰がどのIDを作ったのかを他の利用者から辿れないようにしつつ、運用者が「同じ回線からの発行か」を見分けられる程度に前半を残します |
| `deletion_grace_seconds` | ID・子機URL・外部連携を、発行からこの秒数のあいだ削除・失効できないようにします。ボタンを無効にするだけでなく**サーバー側でも拒否**するので、フォームを直接送っても消せません |
| `id_lifetime` | 発行からこの秒数が経過したIDを自動的に削除します。関連する端末・履歴・子機URL・外部連携も一緒に消えます。残り時間は管理画面に表示されます |

自動削除は cron ではなく、**誰かがアクセスした時点**で判定します（`id_lifetime` が 0 のときは
判定自体を行わないので、通常の設置に負荷はかかりません）。
削除ロックは自動削除には掛かりません（第三者の操作を防ぐための仕組みであり、寿命とは別のため）。


## 実装上の仕様

### 役割と端末の識別

- ログイン後の認証状態は PHP セッション（HttpOnly / SameSite=Lax Cookie）で保持します。
- ブラウザは `localStorage` に**端末キー**を保存し、ログイン時に送信します。
  これにより再ログインしても同じ端末として扱われ、子機の履歴が引き継がれます。
- 端末キーはブラウザ側の値なので、それだけでは同時接続数の上限を守れません。
  ログインのたびにサーバーが**セッション鍵**を発行し直し、1台の端末につき有効なセッションを
  1つに限定しています。同じ端末で入り直すと、前のセッションは自動的にログアウトします。
- 「稼働中」の判定は `polling_interval × parent_offline_polls`（既定 50 秒）以内に
  ポーリングがあったかどうかで行います。ログアウトすると即座に枠が解放されます。

### リングトーン

音源ファイルは使わず、Web Audio API でチャイム音を合成しています。
ブラウザの自動再生制限に対応するため、ログインボタン押下（ユーザー操作）の中で
AudioContext を初期化します。対応端末ではバイブレーションも併用します。

### データベース

| テーブル | 用途 |
| --- | --- |
| `doorbell_ids` | 発行済みID、親機/子機パスワードのハッシュ、発行元IP |
| `devices` | ログイン中の端末（役割・表示名・IP・最終ポーリング時刻） |
| `calls` | 呼び出しと応答（状態・呼び出し回数・応答メッセージ） |
| `child_links` | 子機URLのトークン |
| `integrations` | 外部連携（トークンのハッシュ・通知先URL・署名シークレット） |
| `webhook_events` | 通知の送信待ち（送信できた行は削除される） |
| `rate_limits` | レート制限のカウンタ |

古い履歴（既定 30 日）と長期間アクセスのない端末は、アクセス時にまれに実行される
クリーンアップ処理で自動削除されます。cron の設定は不要です。

## テスト

```bash
bash tests/run.sh          # 構文チェック + 全テスト
php  tests/logic_test.php  # ロジックのみ（113件）
bash tests/e2e_test.sh     # HTTP経由の統合テストのみ（95件）
```

| ファイル | 内容 |
| --- | --- |
| `tests/logic_test.php` | 不在判定、履歴のまとめ、同時コール、子機の稼働状態、各種上限、子機URL、外部連携、IDの正規化 |
| `tests/e2e_test.sh` | ID発行 → 親機/子機ログイン → 呼び出し → 応答までを HTTP 経由で検証。CSRF・権限・連携API・デモ設置用の設定・非公開ファイルの秘匿も確認 |

`examples/` のサンプルコードはテストの対象外です（設置環境に合わせて書き換える前提のため）。

どちらも**一時ファイルの SQLite と一時の設定ファイルを使うため、`data/` の実データや
`config/config.php` の内容には影響されません**（`e2e_test.sh` は専用のテストサーバーを
自動で起動・停止します）。管理パスワードを変更してある環境でもそのまま実行できます。
DB と設定ファイルの場所は環境変数 `DOORBELL_DB_PATH` / `DOORBELL_CONFIG_PATH` で上書きできます
（CLI と組み込みサーバー専用。php-fpm 等の本番環境では無視されます）。

すでに動作しているサーバーに対して統合テストを実行することもできます。
この場合はそのサーバーのデータに ID が追加される点と、**管理パスワードが既定（`1234`）で
あることを前提にしている**点に注意してください。

```bash
BASE_URL=http://127.0.0.1:8000 bash tests/e2e_test.sh
```

## セキュリティと DoS 対策

実装済みの対策:

- 設定ファイル・DB・ソースを公開ディレクトリの外に配置し、`.htaccess` でも直接アクセスを拒否
- SQLite ファイルは所有者だけが読めるパーミッション（`0600`）に調整（POSIX 環境のみ）
- パスワードは `password_hash()` でハッシュ化して保存
- 全ての状態変更操作に CSRF トークンを要求、ログイン成功時にセッションIDを再生成
- IP単位のレート制限（既定: API 300回/分、ログイン試行 10回/分、コール 20回/分）。
  ログイン試行の枠はメイン画面と ID発行画面で別々に数えます
- ID・パスワードの誤りによるログイン失敗時は、IDの存在有無を推測されないよう
  **エラー内容と応答時間の両方**を揃える
- ID発行数の上限（サーバー全体 / IP単位）と同時接続端末数の上限
- ID発行画面のログインは無操作が `admin_session_lifetime` 続くと失効
- 応答メッセージは設定ファイルの定義のみを受け付ける（クライアントから任意の文字列を送れない）
- 外部連携のトークンはハッシュ化して保存し、通知先URLは https と許可ホストに限定（SSRF 対策）
- 出力は全てエスケープ（PHP側は `htmlspecialchars`、JS側は `textContent`）

追加で検討する場合:

- **Basic 認証**: 不特定多数に公開する必要がなければ、システム全体に Basic 認証を掛けるのが
  最も確実です（Apache なら `public/.htaccess` に `AuthType Basic` を追加）。
  ただし、子機を来訪者に操作させる用途では運用が難しくなる点に注意してください。
- **HTTPS 必須化**: パスワードを平文で送らないよう、公開運用では TLS を必須にしてください。
  HTTPS 環境では Cookie に自動的に `Secure` 属性が付きます。
- **リバースプロキシでの制限**: nginx の `limit_req` / Cloudflare 等での追加制限。
- `polling_interval` を大きくすると、同時接続端末が増えたときのサーバー負荷を下げられます。

## 今回実装していない項目（将来拡張？）

- コールボタン押下時に Web カメラで撮影した画像を送信
- ボイスメッセージの録音・再生
