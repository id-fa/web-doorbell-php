# 簡易ドアベルシステム

## 通話機能（テスト版）

親機と子機のあいだで、ブラウザだけでボイスチャットができる機能です。
親機の応答ボタンの横に「通話する」が増え、押すと子機が自動で応じて会話が始まります。

- **音声は双方向**
- **映像は子機 → 親機の片方向**（来訪者の顔が親機に映る。設定で無効にできます）
- 音声・映像はブラウザ同士が直接やり取りします（WebRTC）。サーバーは中継しません

> **テスト版の機能です。**
> 既定では無効になっています。動作条件が厳しく（後述）、環境によっては繋がりません。
> 仕様や設定項目は今後変わる可能性があります。

---

## 動作条件

次の 3 つが揃わないと使えません。**どれか 1 つでも欠けると、通話ボタン自体が表示されません。**

### 1. HTTPS でアクセスできること

ブラウザはマイクとカメラ（`getUserMedia`）を、安全な接続でしか使わせません。
`http://` でアクセスしている場合、通話ボタンは出ません。

- 例外は `http://localhost` / `http://127.0.0.1` です。開発中の動作確認はこれで行えます
- 実機（Android タブレット等）で使うには、正規の証明書か自己署名証明書が必要です

### 2. 親機と子機が直接つながるネットワークにあること

このシステムは中継サーバー（TURN）を持ちません。親機と子機が**同一LAN内**にあり、
互いに直接パケットを送れることが前提です。

Wi-Fi アクセスポイントの**クライアント分離**（プライバシーセパレータ／AP アイソレーション）が
有効になっていると、端末同士が直接通信できないため繋がりません。**この設定を切ってください。**

### 3. 子機でマイクとカメラの使用を許可してあること

子機は無人設置を想定しているため、着信時に権限を求めることができません。
そのため**「呼び出す」ボタンを押した時点で**マイクとカメラを確保します。

初回だけブラウザの確認ダイアログが出るので、**「許可」を選んでください**
（Android Chrome では一度許可すれば以降は聞かれません）。
拒否した場合、呼び出しは通常どおり行えますが通話はできません。

---

## 有効にする

`config/config.php` に次を追加します。

```php
'voice_call' => true,
```

これだけで使えます。同一LAN内であれば、STUN / TURN の設定は不要です。

---

## 使い方

1. **子機**で「呼び出す」を押す
   - マイクとカメラが有効になり、「通話に備えてマイクとカメラが有効になっています」と表示されます
2. **親機**に呼び出しが表示される
   - 通常の応答ボタン（「1分以内に応対します」など）の下に「通話する」が出ます
3. **親機**で「通話する」を押す
   - 子機は確認なしで自動的に応じ、数秒で会話が始まります
4. どちらかが「通話を終える」を押すと終了します

- **通話を始められるのは親機だけです。** 子機や外部連携（Slack 等）からは開始できません
- 通話が成立すると、その呼び出しは応答済みとして決着し、応答履歴に
  `voice_call_message` の文言（既定「ここから通話します」）が残ります
- **接続できなかった場合、呼び出しは応答待ちのまま残ります。**
  親機は通常の応答ボタンに戻って選び直せるので、来訪者が置き去りになりません
- 通話中は画面が消えないようにします（Screen Wake Lock）

---

## 設定項目

| キー | 既定値 | 説明 |
| --- | --- | --- |
| `voice_call` | `false` | 通話機能を有効にするか |
| `voice_call_label` | `'通話する'` | 親機に出す通話ボタンの文言（12文字まで） |
| `voice_call_message` | `'ここから通話します'` | 通話成立時に子機へ出す文言。応答履歴にも残る（60文字まで） |
| `voice_video` | `true` | 子機のカメラ映像を親機へ送るか。`false` で音声のみ |
| `voice_ice_servers` | `[]` | NAT 越えに使う STUN / TURN サーバー（後述） |
| `voice_poll_interval` | `1` | 通話中のポーリング間隔（秒）。SDP と ICE の受け渡しに使う |
| `voice_connect_timeout` | `20` | 接続の確立を諦めるまでの秒数 |
| `voice_max_seconds` | `300` | 1回の通話の上限（秒） |
| `voice_stale_seconds` | `15` | 相手のポーリングが途絶えてから切断とみなすまでの秒数 |
| `rate_limit.max_voice` | `900` | 時間窓あたりの通話用リクエスト上限 |

通話中は 1 秒間隔でポーリングするため、`rate_limit.max_voice` は通常のリクエスト
（`max_requests`）とは別枠で数えます。同じ枠にすると通話が上限を食い潰し、
画面全体のポーリングまで止まってしまうためです。

### 応答ボタンとの関係

通話ボタンは設定 `responses` の応答ボタンとは別系統の**組み込みのボタン**です。
`responses` は 3 件までという上限があるため、通話ボタンはその枠を使いません。

そのため、`responses` のキーに `talk` は使えません（`timeout` と同じ予約語です）。
書いてもその項目は無視されます。

---

## 繋がらないとき

### 通話ボタンが出ない

| 確認すること | 対処 |
| --- | --- |
| `voice_call` が `true` か | `config/config.php` を確認する |
| `https://` でアクセスしているか | `http://`（localhost 以外）では出ません |
| ブラウザが WebRTC に対応しているか | Android Chrome / Safari / Firefox の最近のバージョンを使う |

### ボタンは出るが「通話を開始できませんでした」になる

`voice_connect_timeout`（既定 20 秒）以内に接続できなかった場合です。

1. **Wi-Fi のクライアント分離を切る。** これが最も多い原因です。
   アクセスポイントの設定で「プライバシーセパレータ」「AP アイソレーション」
   「端末間通信の禁止」などの項目を無効にしてください
2. 親機と子機が**同じサブネット**にいるか確認する
3. それでも駄目なら STUN を設定する（次項）

### 子機側で「マイクを使用できないため通話できません」と出る

子機でマイクの使用を拒否しています。ブラウザのサイト設定から許可し直してください。
Android Chrome では、アドレスバー左の鍵アイコン →「権限」から変更できます。

---

## STUN / TURN を使う

同一LAN内での利用を想定しているため、既定では STUN も TURN も使いません。
ネットワークの都合で直接つながらない場合だけ設定します。

```php
'voice_ice_servers' => [
    'stun:stun.example.net:3478',
],
```

`urls` / `username` / `credential` を持つ配列でも書けます。

```php
'voice_ice_servers' => [
    ['urls' => 'turn:turn.example.net:3478', 'username' => 'user', 'credential' => 'pass'],
],
```

> **この値はブラウザにそのまま渡るため、誰でも読めます。**
> TURN の固定の認証情報をここに書かないでください。書けば、そのサーバーを
> 第三者に自由に使われます。TURN を使う場合は、短期の認証情報を発行する仕組みを
> 別途用意し、この機能を拡張してください（現状のリポジトリには含みません）。

インターネット越し（別拠点・モバイル回線）で使う場合は、STUN だけでは繋がらないことが多く、
実質 TURN サーバーが必要になります。

---

## セキュリティ上の扱い

- 通話の SDP / ICE は、**そのセッションの当事者（親機と子機）だけ**が読み書きできます。
  他の端末や他のIDからは、セッションIDを知っていても触れません
- 同じIDで同時に成立する通話は 1 本だけです
- `voice_call` が無効なら、API も通話に関するリクエストを一切受け付けません
- 外部連携（Slack 等）からは通話を開始できません。連携には端末がなく、
  通話の相手になれないためです
- 音声・映像はサーバーを経由しません。サーバーに記録も残りません
  （通話した事実と時刻は応答履歴に残ります）

---

## 実装

| ファイル | 役割 |
| --- | --- |
| `src/Voice.php` | シグナリング（SDP / ICE の受け渡し）と通話の生死の管理 |
| `public/assets/voice.js` | ブラウザ側の WebRTC 処理。`app.js` から組み立てて使う |

サーバーは `voice_sessions` / `voice_signals` の 2 テーブルを使います。
呼び出し（`calls`）のライフサイクルには手を入れず、通話は並走するセッションとして持ち、
**接続が成立した時点で**通常の応答と同じ形（`answered` / `response_key = 'talk'`）に
着地させています。成立するまで呼び出しを応答待ちのまま残すのは、接続に失敗したときに
通常の応答へ戻れるようにするためです。

cron は使いません。接続の時間切れ・通話時間の上限・相手の途絶は、
すべて誰かがポーリングした時点で評価されます。

### テスト

```bash
bash tests/run.sh
```

`tests/logic_test.php` と `tests/e2e_test.sh` に通話のセクションがあります。
ただし WebRTC 本体（`getUserMedia` と ICE）はブラウザがないと動かないため、
自動テストで確かめているのは**シグナリングの受け渡しと呼び出しの決着**までです。
実際に音が通るかどうかは、実機またはブラウザでの手動確認が必要です。

手元で確認するときは、`localhost` が安全な接続として扱われることを利用します。

```bash
php -S 127.0.0.1:8791 -t public
```

親機と子機は同じブラウザプロファイルでは同時にログインできません
（セッション Cookie を共有するため）。別のブラウザかシークレットウィンドウを使ってください。
